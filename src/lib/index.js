const {
  closeCache,
  connectCache,

  getItem,
  hasItem,
  removeItem,
  setItem,
} = require('./redis');

module.exports = {
  closeCache,
  connectCache,

  getItem,
  hasItem,
  removeItem,
  setItem,
};
